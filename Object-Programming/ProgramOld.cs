using System;
using System.Collections.Generic;
using System.Text;

namespace Object_Programming
{
    internal class ProgramOld
    {


        //using Object_Programming.Business;
        //using System.Runtime.Serialization;


        //Beer drinkus = new ExpiringBeer("ie", 1, 3, new DateTime(2005-34-23), 3);
        //Beer drink = new Beer("prueba", 43, 3, 3);
        //Services ser = new Services(3, 5); 

        //ISalable[] concepts = [
        //    ser,
        //    drink,
        //    drinkus
        //    ];

        //Console.WriteLine(getTotal(concepts));


        ////Collection<int> colection = new Collection<int>(3); 
        //var elements = new Collection<int>(3);

        //elements.Add(1);
        //elements.Add(2);
        //elements.Add(3);
        //elements.Add(4);
        //elements.Add(5);

        //Console.WriteLine(elements.GetValues()); 

        ////foreach (var concept in elements.GetValues())
        ////{
        ////    Console.WriteLine(concept); 
        ////}


        // int getTotal(ISalable[] concepts)
        //{

        //    int total = 0; 
        //    foreach (var concept in concepts)
        //    {
        //        total += concept.getPrice();
        //        Console.WriteLine(total);
        //    }
        //    return total;
        //}

        //Func<int, int, string> multString = (a, b) =>
        //{
        //    int res = a + b; 
        //    return "prueba: " + res; 
        //};

        //Console.WriteLine(multString(3, 5)); 

        //CLASE 34 FUNCIONES DE ORDEN SUPERIOR


        //List<int> numbers = [1, 2, 3, 4, 5, 6, 7];

        //var numberList2 = Filter(numbers, number => number % 2 == 0); 


        //foreach (var number in numberList2)
        //{
        //    Console.WriteLine(number);
        //}

        //List<int> Filter(List<int> list, Func<int, bool> condition)
        //{

        //    var resultList = new List<int>(); 

        //    foreach (var item in list)
        //    {
        //        if (condition(item)){
        //            resultList.Add(item);
        //        }
        //    }
        //    return resultList; 

        //}
    }
}
